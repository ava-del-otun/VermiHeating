# Execution Documentation Suite

This directory contains a repository-grounded audit and documentation set for the execution graph rooted at `Heat Transfer Study/heat_transfer_study.py`.

## What was generated

- `00_execution_index.tex`
- `code/heat_transfer_study_py.tex`
- `code/analyze_vermicomposter_heater_py.tex`
- `code/vermicomposter_air_heater_design_model_m.tex`
- `code/generated_runtime_matlab_export_helper_m.tex`
- `physics/physics_and_math_reference.tex`
- `physics/implementation_traceability.tex`
- this `README.md`

## Executed code files documented

- `Heat Transfer Study/heat_transfer_study.py` as the verified Python entrypoint.
- `analyze_vermicomposter_heater.py` as a conditionally executed subprocess target on the refresh/export path.
- `vermicomposter_air_heater_design_model.m` as the MATLAB model reached from the export helper.
- The generated temporary MATLAB export helper as a runtime-created executed helper with no stable repository-relative path.

## Conditional code files and paths

- `analyze_vermicomposter_heater.py` only runs when `auto_refresh_export_if_needed(...)` decides the current heating export is stale or missing.
- `vermicomposter_air_heater_design_model.m` only runs if the refresh/export path launches MATLAB successfully.
- The generated runtime MATLAB helper exists only for the duration of the MATLAB export subprocess.
- MATLAB parallel execution and Python multiprocessing are runtime-gated and environment-dependent.

## Non-code runtime inputs identified

- `Heat Transfer Study/study_config.json`
- `Heat Transfer Study/data/model_export_grid100.json`
- `python_heater_analysis_moisture/model_export.json`
- `Heat Transfer Study/outputs/*`

## Formulas and correlations traced into code

- Ideal-gas and Sutherland-law air properties.
- Churchill-Bernstein crossflow convection.
- Laminar and turbulent internal tube Nusselt closures.
- Heater-tube and aeration-tube segment energy balances.
- Lewis-relation evaporation and Clausius-Clapeyron latent-heat treatment.
- Bin-wall loss model, top-mode UA variants, and the two-node bed solve.
- Heating hard constraints, NSGA-II objective vector, and report-side ranking key.
- Cooling hard constraints, cooling NSGA-II objective vector, and report-side Pareto selection order.
- Cooling surrogate relations for assist blower, spot cooler, condensate-limited RH update, and summer airflow sweep.
- Annual climate kernel regression and day-level duty / energy scaling.

## Unresolved ambiguities

- MATLAB batch success and toolbox availability are external to the repository; only the orchestration path is verified.
- The concrete generated helper filename/path is runtime-generated by `tempfile.NamedTemporaryFile(...)`.
- Primary literature PDFs for the repository-native citation list `[R1]` through `[R12]` were not found in the repository tree.

## Missing or weakly documented sources

- `Heat Transfer Study/VermiHeatLoss_complete_updated.tex` is referenced from `VermiHeatLoss.tex` but is missing.
- `Heat Transfer Study/vermi_water_optimization_numerics_appendix.tex` is referenced from `VermiHeatLoss.tex` but is missing.
- Citation page-level verification is limited to whatever detail is preserved in repository comments and TeX narrative.

## Code paths not fully verified

- A full live MATLAB export run was not replayed during this audit.
- Runtime behavior behind MATLAB `parpool(...)` depends on the local MATLAB environment.
- The generated temporary helper was documented from its template and launch code, not from a captured temp file on disk.

## Suggested next verification steps

- Run `.\docs\execution_docs\build_execution_docs.ps1` after any future equation-label edits. The script compiles `physics/physics_and_math_reference.tex` and `physics/implementation_traceability.tex` twice so equation references and bookmarks settle cleanly.
- If cooling feasibility is intended to require full cooling-load satisfaction, decide whether `coolingShortfall_W` should become a hard cooling constraint instead of remaining diagnostic-only.
- Compile the generated `.tex` files in a local LaTeX environment.
- Capture one real refresh/export run with MATLAB available and archive the emitted temp helper for comparison against the documented template.
- Decide whether the MATLAB-side `explicitlySafe(...)` summary helper should be updated to match the Python-side max-bed-temperature hard constraints exactly.
- Add local copies of the primary literature sources if page-by-page citation verification is required.
