Vermicomposter Heater Python Analysis
====================================

Active configuration:
- open top

Recommended point used throughout:
- Pitch: 6.0 mm
- Voltage: 72.3 V
- Total flow: 400.0 L/min
- Total power: 417.2 W
- Total current: 5.77 A

What "recommended" means:
- Recommended = the single operating point selected for a vessel configuration from the MATLAB sweep. The code first prefers fully feasible points, then current-safe fallback points, then the lowest-penalty point overall.

Why the same star appears on every constraint/performance plot:
- The star marks the same comparison-based recommended operating point on every constraint/performance panel because all panels share the same voltage-flow plane at one pitch; only the plotted response variable changes.
- In this case the starred point is 6.0 mm, 72.3 V, 400.0 L/min, so every panel shows the same x-y location while the z-value changes by metric.

300-point display note:
- Continuous Python plots were redrawn at 100 display points per axis where that makes sense.
- Vessel configuration bars remain discrete because there are only three actual vessel configurations in the model.
- MATLAB model grid override: 100.
- Python figures use interpolated display grids; they do not change the underlying MATLAB physics unless model_grid_points is set.
